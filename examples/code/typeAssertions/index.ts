/**
 * Function that casts an input to a string and returns its length.
 * @param {any} input The input.
 * @returns {number} The length of the input string.
 */
function getInputLength(input: any): number {
    const str = input as string;
    return str.length;
}
