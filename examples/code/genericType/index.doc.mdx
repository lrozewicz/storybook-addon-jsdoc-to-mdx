import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="code/genericType" />

# identity

`code/genericType`
**AST Node Type:** *FunctionDeclaration*

> Generic function that returns the argument.

#### Parameter:

- `arg` *T* — The argument of type T.
#### Returns:
`T`  argument of type T.
#### Template:
- `T` —

#### Source Code:

```ts
function identity<T>(arg: T): T {
    return arg;
}

```

